## Operator gate

This card is the operator's. It stays blocked until they answer; a worker
never completes it.

- Artifacts (absolute paths): <fill>
- Open them: `<one copy-pasteable command>`
- Questions (answer by number):
  1. <fill>

The coordinator records the verdict on the ticket and runs
`hermes kanban --board {{BOARD}} complete <this-id> --summary "<verdict>"`.
