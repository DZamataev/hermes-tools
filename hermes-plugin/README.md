# Hermes OpenWebUI plugin

This directory is reserved for the update-safe user plugin loaded from
`~/.hermes/plugins/openwebui-bridge`.

The plugin will run inside the Hermes Desktop backend, submit messages to the
already active session ID, and expose replayable turn events to the bridge
without patching the installed Hermes application.

Implementation starts after the bridge protocol design is approved.

